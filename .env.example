# Example environment variables for local usage (copy to .env and edit)
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=5432
PG_DB=filehash

JWT_SECRET=defaultsecretkey

# DATABASE_URL examples:
# Using DSN
DATABASE_URL=host=localhost user=postgres password=5432 dbname=filehash port=5432 sslmode=disable
# Or using URL
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/filehash?sslmode=disable

APP_PORT=8080
